--- Changes attempted for scx_gamer BPF optimization ---

1. Force GPU threads to physical cores (avoid SMT)
   Location: pick_idle_cpu_cached() around line 977
   
   Change:
   struct task_ctx *tctx = cache ? cache->tctx : try_lookup_task_ctx(p);
   bool is_critical_gpu = tctx && tctx->is_gpu_submit;
   bool allow_smt = is_critical_gpu ? false :
                    (!avoid_smt || (!is_busy && interactive_sys_avg < INTERACTIVE_SMT_ALLOW_THRESH));
   
   Issue: BPF verifier rejected accessing cache->tctx (new field added to struct)

2. CPU affinity stickiness for high-CPU threads
   Location: gamer_select_cpu() around line 1561
   
   Change:
   struct task_ctx *tctx = try_lookup_task_ctx(p);
   if (is_fg && tctx &&
       tctx->exec_avg > 50000ULL &&
       tctx->wakeup_freq > 100 &&
       bpf_cpumask_test_cpu(prev_cpu, p->cpus_ptr)) {
       // keep on prev_cpu
   }
   
   Issue: BPF verifier rejected bpf_cpumask_test_cpu(prev_cpu, p->cpus_ptr)
          - p->cpus_ptr is not a trusted pointer for BPF

3. Added stat counter
   Location: line 185
   volatile u64 nr_affinity_kept;

4. Added field to struct pick_cpu_cache
   Location: line 902
   struct task_ctx *tctx;

--- End of changes ---
